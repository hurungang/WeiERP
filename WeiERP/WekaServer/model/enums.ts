export const enum HTTPStatusCode{
  OK=200,
  BadRequest=400,
  InternalServerError=500,
}

export const enum MessageSectionCategory{
  Name,
  Address,
  Mobile,
  CommodityName,
  Quantity,
  Other
}