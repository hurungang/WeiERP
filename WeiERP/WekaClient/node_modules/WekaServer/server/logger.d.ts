import 'winston-daily-rotate-file';
export default class Logger {
    private logger;
    constructor(label: string);
    info(logContent: string): void;
    error(logContent: string): void;
    debug(logContent: string): void;
}
