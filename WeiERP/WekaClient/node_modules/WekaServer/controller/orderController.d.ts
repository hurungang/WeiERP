import * as express from 'express';
import { IController, Controller } from './controller';
export default class OrderController extends Controller implements IController {
    constructor();
    create(req: express.Request, res: express.Response, next: express.Next): void;
    list(req: any, res: any, next: any): void;
    getById(req: any, res: any, next: any): void;
    update(req: any, res: any, next: any): void;
    deleteById(req: any, res: any, next: any): void;
}
