import * as express from 'express';
import { Controller } from './controller';
export default class ChatController extends Controller {
    constructor();
    chat(req: express.Request, res: express.Response, next: express.Next): void;
}
