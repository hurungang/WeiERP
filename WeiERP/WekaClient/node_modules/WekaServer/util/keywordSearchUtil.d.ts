export default class KeywordSearchUtil {
    matchedIndexArray: number[];
    constructor(content: string, strKeys: string);
    getMatchCounts(): number;
    private makeTree(strKeys);
    private search(content, tblRoot);
}
